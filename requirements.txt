# Core dependencies for IWS mode
WTForms~=3.2.1          # Form generation and validation
Jinja2~=3.1.6           # Template rendering
Werkzeug~=3.1.3         # MultiDict for WTForms
MarkupSafe~=3.0.2       # Jinja2 dependency
python-dotenv~=1.1.1    # Environment configuration
pytest~=8.4.1           # Testing

# Removed Flask dependencies (migrated to IWS):
# Flask~=3.1.2          - Replaced by Indigo Web Server
# Flask-WTF~=1.2.2      - Using WTForms directly
# requests~=2.32.5      - Using direct indigo objects
# click, blinker, itsdangerous, certifi, urllib3, idna, charset-normalizer